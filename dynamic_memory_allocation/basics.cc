#include <iostream>
using namespace std;

int main()
{
    // normal memory allocation
    int x = 10;

    // dynamic memory allocation
    int *ptr = new int;

    // here ptr is a pointer which is stores the address of the memory location user is going to store , but that memory is in heap memory unlike the static memory allocation in normal declaration
}