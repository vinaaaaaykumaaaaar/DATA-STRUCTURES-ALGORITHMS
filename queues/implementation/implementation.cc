#include <iostream>
using namespace std;

/*
| Operation    | Meaning            | Complexity |
| ------------ | ------------------ | ---------: |
| `enqueue(x)` | Add to rear        |       O(1) |
| `dequeue()`  | Remove from front  |       O(1) |
| `front()`    | See first element  |       O(1) |
| `empty()`    | Is queue empty?    |       O(1) |
| `size()`     | Number of elements |       O(1) |

*/

// Native array implementation

class Queue
{
private:
    int *arr;
    int capacity;

    int frontIndex;
    int rearIndex;
    int count;

public:
};

int main()
{
}