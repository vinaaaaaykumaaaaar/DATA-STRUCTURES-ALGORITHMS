#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
#include <map>
#include <stack>
#include <queue>
#include <list>

using namespace std;

void solve(vector<int> &a)
{

    vector<int> ans(a.size(), 0);
}

int main()
{

    vector<int> a = {3, 1, -2, -5, 2, -4};
    int n, m, i, k;

    solve(a);

    return 0;
}

// 3 1 -2 -5 2 -4 -> -2 3 -5 1 -4 2